"""ERP benchmark package — Quality Arena (RQ1-3).

Modules:
    datasets   loaders for official CLASSIX shape sets + UCI Real_data
    methods    runners for KMeans++, DBSCAN, Hierarchical (Ward), CLASSIX
    metrics    quality + safe-handling helpers
    run_benchmark   orchestrator
"""
